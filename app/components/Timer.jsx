var React = require('react');

var Timer = React.createClass({
    render: function() {
        return (
            <div>
                This is the Timer.jsx
            </div>

        );
    }
});

module.exports = Timer;